import 'package:flutter/material.dart';
import 'package:memory_game/presentation/model/modal_kind.dart';

class CommonModal extends StatelessWidget {
  final String modalText;
  final ModalKind modalKind;
  const CommonModal(
      {super.key, required this.modalText, required this.modalKind});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
